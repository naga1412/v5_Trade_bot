// SP-0.5 Phase L2: route placeholder. Full Bot Status panels land in later
// phases (M+) — this exists so the hash route resolves and the TabNav has
// somewhere to navigate to.

export function BotStatus() {
  return (
    <div className="h-full flex items-center justify-center bg-bg-base text-text-secondary font-mono text-sm">
      Bot Status (under construction)
    </div>
  );
}
