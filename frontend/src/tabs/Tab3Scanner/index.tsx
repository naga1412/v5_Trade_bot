// SP-6 Phase A6: Tab 3 Scanner stub. Real implementation lands in Phase D
// (radar grid + filters + per-card sparkline). For now this keeps the route
// reachable so navigation, hash deeplinks, and Lighthouse can exercise the
// 5-tab nav before the full UI is wired.

export function Tab3Scanner() {
  return (
    <div className="p-4 text-text-secondary">
      Scanner — coming in Phase D
    </div>
  );
}
