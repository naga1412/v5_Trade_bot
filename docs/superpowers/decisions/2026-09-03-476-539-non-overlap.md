# #476 and #539 both touch `live_prediction.py` but never collide

**Class:** conflict-reconciliation record for a flagged forward-reference. **Do not amend by squash.** Any future change to this decision requires a new decision record replayed in order.

## The concern

Both this session's own decision doc (`2026-09-01-staged-promotion-shared-file-audit.md`) and the operator's explicit instruction for Stage 2's cherry-pick named this pairing as needing real reconciliation, not a mechanical merge: "#476 specifically shares run_live_prediction with #539... diff #476 against post-#539 main before assuming a clean cherry-pick." The instruction was to read both diffs and confirm the merged result preserves BOTH #539's futures-REST seed branching and #476's dispatch-time liquidity recheck — not to silently pick one side if git auto-merged without conflict markers.

## What each PR actually touches

**#539** (`fix(futures-poll): seed history from futures REST, not spot`) modifies `run_live_prediction()`'s SEED step — specifically the branch at

```python
if symbol_source == "futures_poll":
    ...  # fetch_futures_seed_klines(...)
```

which lives inside `run_live_prediction`, the coroutine that seeds in-memory kline history before a symbol's WS stream starts.

**#476** (`feat(phase4): Task 10 — dispatch-time liquidity re-check + card suppression`) modifies `_maybe_dispatch()` — a **separate function**, called after a prediction is built, right before deciding whether to send a Telegram card / execute a live order:

```python
if symbol_source != "established_top20":
    try:
        rate_client = get_intermarket_adapter().rate_client
        check = await check_liquidity(pred.symbol.replace("/", ""), rate_client)
    except Exception as e:
        ...  # suppress, log
    if not check.passed:
        ...  # suppress, log
```

Both functions live in the same file (`backend/app/ws/live_prediction.py`) and both key off the same `symbol_source` parameter, which is why the operator's forward-reference warning was the right instinct to check carefully — but they operate at **different points in the pipeline** (seed-time vs. dispatch-time) and touch **disjoint line ranges**. `#476`'s only file-level interaction with `#539`'s work was a shared import block (both added a new import in the same alphabetical position), which produced exactly one trivial two-line conflict — resolved by keeping both imports, no semantic content lost.

## Verification (not assumed — checked directly on the built squash)

```
$ grep -n "^async def \|symbol_source == \"futures_poll\"\|symbol_source != \"established_top20\"" backend/app/ws/live_prediction.py
129:async def run_live_prediction(
163:    if symbol_source == "futures_poll":
410:async def _maybe_dispatch(
429:    if symbol_source != "established_top20":
```

Both branches are present, in their own functions, doing their own jobs, with `_maybe_dispatch` (429) sitting 247 lines below `run_live_prediction`'s seed branch (163) — no overlap, no clobbering, no silent pick of one side.

## Why this reads as "should conflict" but doesn't

`symbol_source` is a single parameter threaded through `run_live_prediction`'s whole call chain (seed → predict → persist → dispatch), so two PRs that both branch on its value anywhere in that chain look, from the PR title alone, like they're touching "the same logic." They're not — they're two independent decision points that happen to share the same routing variable, the way two functions can both check `if user.is_admin:` without needing to coordinate on what each does inside that branch. The actual git-level conflict was limited to the import block; the two behavioral branches were never on colliding lines.

## Reversal criteria

Re-examined if a future cherry-pick of either PR's descendants (e.g., a follow-up to #476's liquidity-recheck logic) produces a REAL line-level conflict against `run_live_prediction`'s seed branch — that would mean the two code paths have since grown closer together and this record's "disjoint, no coordination needed" conclusion no longer holds.
