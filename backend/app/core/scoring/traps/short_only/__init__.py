"""Short-only trap subpackage (SP-5 Phase D).

Each module in this package implements a `Trap` (per `traps/base.py`) that
fires only when the proposed direction is `SHORT`. Registered into
`ALL_TRAPS` from `app/core/scoring/traps/__init__.py`.
"""
